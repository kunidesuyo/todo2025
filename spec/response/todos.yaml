getResponse:
  description: OK
  content:
    application/json:
      schema:
        type: array
        items:
          type: object
          properties:
            id: { $ref: "../schema/todo.yaml#/id" }
            title: { $ref: "../schema/todo.yaml#/title" }
            completed: { $ref: "../schema/todo.yaml#/completed" }
